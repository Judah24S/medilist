export interface Medicine{
    medicineId ?: number;
    name ?: string;
    dosage ?: string;
    prescribingDoctor ?: string;
    dateIssued ?: Date;
    reason ?: string;
    timesToTake ?: string;
}